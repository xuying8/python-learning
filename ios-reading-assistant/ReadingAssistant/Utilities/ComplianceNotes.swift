import Foundation

enum ComplianceNotes {
    static let policy = """
    This app only supports user-imported EPUB/TXT files and metadata search.
    It does not scrape, index, or download copyrighted books from piracy websites.
    """
}
